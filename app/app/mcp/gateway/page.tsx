import { NextGenWorkspace } from "@/components/platform-next";

export default function MCPGatewayPage() {
  return (
    <NextGenWorkspace
      pageTitle="MCP Gateway"
      pageDescription="Gateway routing, policy controls, traffic governance, and secure protocol mediation for enterprise MCP connectivity."
      centerLabel="AIOS MCP"
      emphasis="roadmap"
      domain="mcp"
    />
  );
}
