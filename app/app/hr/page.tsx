import { NextGenWorkspace } from "@/components/platform-next";

export default function HRPage() {
  return (
    <NextGenWorkspace
      pageTitle="HR Intelligence Center"
      pageDescription="Employee lifecycle, org chart topology, open hiring pipeline, retention risk, performance signals, skills matrix, and culture health indicators."
      centerLabel="AIOS HR Intelligence"
      domain="hr"
    />
  );
}
