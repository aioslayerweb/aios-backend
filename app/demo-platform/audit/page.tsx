import { NextGenWorkspace } from "@/components/platform-next";

export default function DemoPage() {
  return (
    <NextGenWorkspace
      pageTitle="Audit Intelligence Center"
      pageDescription="End-to-end audit visibility for decisions, policy actions, approvals, and autonomous tasks."
      centerLabel="AIOS Demo"
      domain="audit"
    />
  );
}
