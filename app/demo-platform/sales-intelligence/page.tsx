import { NextGenWorkspace } from "@/components/platform-next";

export default function DemoPage() {
  return (
    <NextGenWorkspace
      pageTitle="Sales Intelligence Center"
      pageDescription="Pipeline health, revenue forecast, account signals, deal probability, and AI next-best-action."
      centerLabel="AIOS Demo"
      domain="sales"
    />
  );
}
