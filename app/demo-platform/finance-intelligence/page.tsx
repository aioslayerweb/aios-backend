import { NextGenWorkspace } from "@/components/platform-next";

export default function DemoPage() {
  return (
    <NextGenWorkspace
      pageTitle="Finance Intelligence Center"
      pageDescription="Revenue, cash flow, profit, forecast, budget, margins, anomaly detection, and financial health."
      centerLabel="AIOS Demo"
      domain="finance"
    />
  );
}
