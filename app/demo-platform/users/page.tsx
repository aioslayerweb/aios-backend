import { NextGenWorkspace } from "@/components/platform-next";

export default function DemoPage() {
  return (
    <NextGenWorkspace
      pageTitle="User Intelligence Center"
      pageDescription="User profiles, identity context, workspace assignment, and operational ownership views."
      centerLabel="AIOS Demo"
      domain="users"
    />
  );
}
