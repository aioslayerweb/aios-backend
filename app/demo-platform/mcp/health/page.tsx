import { NextGenWorkspace } from "@/components/platform-next";

export default function DemoPage() {
  return (
    <NextGenWorkspace
      pageTitle="MCP Health"
      pageDescription="Platform health diagnostics, readiness scoring, latency posture, and reliability analytics."
      centerLabel="AIOS Demo"
      domain="mcp"
      emphasis="roadmap"
    />
  );
}
