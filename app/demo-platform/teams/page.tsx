import { NextGenWorkspace } from "@/components/platform-next";

export default function DemoPage() {
  return (
    <NextGenWorkspace
      pageTitle="Team Performance Center"
      pageDescription="Cross-functional topology, accountability mapping, delivery velocity, and collaboration signals."
      centerLabel="AIOS Demo"
      domain="teams"
    />
  );
}
