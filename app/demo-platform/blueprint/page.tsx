import { NextGenWorkspace } from "@/components/platform-next";

export default function DemoPage() {
  return (
    <NextGenWorkspace
      pageTitle="Business Blueprint Center"
      pageDescription="Company profile, OKRs, capabilities, ecosystem, AI readiness, confidence score, and blueprint approval workflow."
      centerLabel="AIOS Demo"
      domain="blueprint"
    />
  );
}
