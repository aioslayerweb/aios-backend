import { NextGenWorkspace } from "@/components/platform-next";

export default function DemoPage() {
  return (
    <NextGenWorkspace
      pageTitle="Permission Governance Center"
      pageDescription="Policy-driven permission boundaries for humans, AI operators, and workflows."
      centerLabel="AIOS Demo"
      domain="permissions"
    />
  );
}
