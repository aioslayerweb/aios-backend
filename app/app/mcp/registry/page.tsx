import { WorkspacePlaceholder } from "../../_components/workspace-placeholder"

export default function MCPRegistryPage() {
  return (
    <WorkspacePlaceholder
      title="MCP Registry"
      description="Central MCP registry operations and package indexing are currently in the roadmap queue."
      comingSoon
    />
  )
}
