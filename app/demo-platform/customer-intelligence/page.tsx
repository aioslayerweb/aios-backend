import { NextGenWorkspace } from "@/components/platform-next";

export default function DemoPage() {
  return (
    <NextGenWorkspace
      pageTitle="Customer Intelligence Center"
      pageDescription="360 customer profile, health, journey, revenue, support, churn risk, lifetime value, and next-best-action."
      centerLabel="AIOS Demo"
      domain="customer"
    />
  );
}
