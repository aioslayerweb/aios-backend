import { NextGenWorkspace } from "@/components/platform-next";

export default function DemoPage() {
  return (
    <NextGenWorkspace
      pageTitle="Organization Intelligence Center"
      pageDescription="Entity hierarchy, ownership intelligence, capabilities, and relationship signals."
      centerLabel="AIOS Demo"
      domain="organization"
    />
  );
}
