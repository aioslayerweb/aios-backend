import { NextGenWorkspace } from "@/components/platform-next";

export default function DemoPage() {
  return (
    <NextGenWorkspace
      pageTitle="Settings Center"
      pageDescription="Organization, users, roles, notifications, billing, security, intelligence capacity, and enterprise preferences."
      centerLabel="AIOS Demo"
      domain="api-keys"
    />
  );
}
