import { NextGenWorkspace } from "@/components/platform-next";

export default function DemoPage() {
  return (
    <NextGenWorkspace
      pageTitle="Operations Intelligence Center"
      pageDescription="Projects, resources, capacity, incidents, assets, supply chain, productivity, and utilisation."
      centerLabel="AIOS Demo"
      domain="operations"
    />
  );
}
