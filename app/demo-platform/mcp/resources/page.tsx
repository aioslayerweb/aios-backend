import { NextGenWorkspace } from "@/components/platform-next";

export default function DemoPage() {
  return (
    <NextGenWorkspace
      pageTitle="MCP Resources"
      pageDescription="Resource orchestration, dependency controls, lifecycle status, and sharing policies."
      centerLabel="AIOS Demo"
      domain="mcp"
      emphasis="roadmap"
    />
  );
}
