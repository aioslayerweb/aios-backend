import { NextGenWorkspace } from "@/components/platform-next";

export default function DemoPage() {
  return (
    <NextGenWorkspace
      pageTitle="Runtime Center"
      pageDescription="Runtime reliability, execution controls, health diagnostics, and environment orchestration."
      centerLabel="AIOS Demo"
      domain="runtime"
      emphasis="roadmap"
    />
  );
}
