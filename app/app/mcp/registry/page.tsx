import { NextGenWorkspace } from "@/components/platform-next";

export default function MCPRegistryPage() {
  return (
    <NextGenWorkspace
      pageTitle="MCP Registry"
      pageDescription="Central MCP registry operations, package indexing, capability cataloging, and governance metadata for connected tool ecosystems."
      centerLabel="AIOS MCP"
      emphasis="roadmap"
      domain="mcp"
    />
  );
}
