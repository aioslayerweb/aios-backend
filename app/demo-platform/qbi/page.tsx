import { NextGenWorkspace } from "@/components/platform-next";

export default function DemoPage() {
  return (
    <NextGenWorkspace
      pageTitle="QBI Center"
      pageDescription="Monte Carlo simulation, probability mapping, forecasting, scenario modelling, confidence scoring, optimization."
      centerLabel="AIOS Demo"
      domain="qbi"
    />
  );
}
