import { NextGenWorkspace } from "@/components/platform-next";

export default function DemoPage() {
  return (
    <NextGenWorkspace
      pageTitle="MCP Prompts"
      pageDescription="Prompt lifecycle tooling with governance, quality scoring, and reusable contract management."
      centerLabel="AIOS Demo"
      domain="mcp"
      emphasis="roadmap"
    />
  );
}
