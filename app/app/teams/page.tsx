import { NextGenWorkspace } from "@/components/platform-next";

export default function TeamsPage() {
  return (
    <NextGenWorkspace
      pageTitle="Team Performance Center"
      pageDescription="Cross-functional team topology, accountability mapping, delivery velocity, collaboration signals, and department alignment for executive operations."
      centerLabel="AIOS Teams"
      domain="teams"
    />
  );
}
