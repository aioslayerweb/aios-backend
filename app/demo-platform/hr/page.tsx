import { NextGenWorkspace } from "@/components/platform-next";

export default function DemoPage() {
  return (
    <NextGenWorkspace
      pageTitle="HR Intelligence Center"
      pageDescription="Employees, org chart, hiring pipeline, retention risk, performance, skills matrix, and culture indicators."
      centerLabel="AIOS Demo"
      domain="hr"
    />
  );
}
