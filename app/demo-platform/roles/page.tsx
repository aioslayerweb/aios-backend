import { NextGenWorkspace } from "@/components/platform-next";

export default function DemoPage() {
  return (
    <NextGenWorkspace
      pageTitle="Role Intelligence Center"
      pageDescription="Role-based intelligence, responsibility mapping, confidence indicators, and workload balance."
      centerLabel="AIOS Demo"
      domain="roles"
    />
  );
}
