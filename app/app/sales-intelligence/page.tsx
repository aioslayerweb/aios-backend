import { NextGenWorkspace } from "@/components/platform-next";

export default function SalesIntelligencePage() {
  return (
    <NextGenWorkspace
      pageTitle="Sales Intelligence Center"
      pageDescription="Pipeline health, revenue forecast, account signals, deal probability, AI next-best-action, and sales velocity across every stage of the enterprise revenue machine."
      centerLabel="AIOS Sales Intelligence"
      domain="sales"
    />
  );
}
