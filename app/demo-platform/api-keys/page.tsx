import { NextGenWorkspace } from "@/components/platform-next";

export default function DemoPage() {
  return (
    <NextGenWorkspace
      pageTitle="API Key Security Center"
      pageDescription="Secure API credential lifecycle, governance-ready key posture, and policy context."
      centerLabel="AIOS Demo"
      domain="api-keys"
    />
  );
}
