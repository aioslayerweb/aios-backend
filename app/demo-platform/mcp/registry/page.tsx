import { NextGenWorkspace } from "@/components/platform-next";

export default function DemoPage() {
  return (
    <NextGenWorkspace
      pageTitle="MCP Registry"
      pageDescription="Central MCP registry operations, package indexing, capability cataloging, and governance metadata."
      centerLabel="AIOS Demo"
      domain="mcp"
      emphasis="roadmap"
    />
  );
}
