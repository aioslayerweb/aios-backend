import { NextGenWorkspace } from "@/components/platform-next";

export default function DemoPage() {
  return (
    <NextGenWorkspace
      pageTitle="Developer Center"
      pageDescription="API, SDK, MCP tooling, logs, testing, documentation, tokens, and rate-limit visibility."
      centerLabel="AIOS Demo"
      domain="developer"
      emphasis="roadmap"
    />
  );
}
