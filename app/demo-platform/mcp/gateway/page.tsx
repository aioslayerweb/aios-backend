import { NextGenWorkspace } from "@/components/platform-next";

export default function DemoPage() {
  return (
    <NextGenWorkspace
      pageTitle="MCP Gateway"
      pageDescription="Gateway routing, policy controls, traffic governance, and secure protocol mediation."
      centerLabel="AIOS Demo"
      domain="mcp"
      emphasis="roadmap"
    />
  );
}
